# Homebrew Tap: prklm10

This is the official custom Homebrew Tap for tools developed by [@prklm10](https://github.com/prklm10). 

Currently, this tap hosts the formula for **[ContextRecall](https://github.com/prklm10/contextrecall)**: a contextual, directory-specific shell history manager written in Rust.

## 📦 Available Formulae

* `contextrecall`: Stop scrolling through global shell history. Give every project its own isolated memory.

## 🚀 How to Install

You can install `contextrecall` using Homebrew on macOS or Linux by running:

```bash
# 1. Add this tap to your local Homebrew
brew tap prklm10/tap

# 2. Install the tool
brew install contextrecall
